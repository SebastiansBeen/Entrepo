<%= form_for(a) do |f| %>
  <% if a.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(a.errors.count, "error") %> prohibited this a from being saved:</h2>

      <ul>
      <% a.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
